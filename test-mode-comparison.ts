// Test file to compare template vs AST transformations
var clickCount = 0;
var isEnabled = true;

function exampleFunction() {
  // String concatenation  
  const msg = 'Welcome ' + username + ' to the app';
}
