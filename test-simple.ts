// Simple file to test var transformations
var userName = 'John';
var userAge = 25;
var isActiveUser = true;

function test() {
  if (userName === 'John') {
    console.log('Error: Found John');
  }
}
