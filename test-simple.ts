// Simple file to test var transformations
const userName = 'John';;
const userAge = 25;;
const isActiveUser = true;;

function test() {
  if (userName === 'John') {
    console.log('Error: Found John');
  }
}
