# Prometheus configuration for TensorRT Oracle system
global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "tensorrt_rules.yml"
  - "/etc/prometheus/rules/*.yml"

scrape_configs:
  # TensorRT Oracle Application metrics
  - job_name: 'tensorrt-oracle'
    static_configs:
      - targets: ['tensorrt-oracle:8080']
    metrics_path: '/metrics'
    scrape_interval: 15s
    scrape_timeout: 10s

  # PostgreSQL metrics
  - job_name: 'postgres'
    static_configs:
      - targets: ['postgres:5432']
    metrics_path: '/metrics'
    scrape_interval: 10s

  # Prometheus self-monitoring
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Node exporter (if added later)
  - job_name: 'node'
    static_configs:
      - targets: ['node-exporter:9100']
    scrape_interval: 5s

alerting:
  alertmanagers:
    - static_configs:
        - targets:
          - alertmanager:9093